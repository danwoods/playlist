var resourceful = require('../lib/resourceful');
resourceful.use('memory');

var Author = resourceful.define('author');
Author.string('name');

console.log(Author.schema);

Author.create({
  id: 'Marak'
}, function(err, marak){
  console.log(marak);
});
